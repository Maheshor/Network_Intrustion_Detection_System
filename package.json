{
  "name": "nidds",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "electron": "wait-on http://localhost:5173 && cd electron && npx electron .",
    "dev": "concurrently \"npm run frontend\" \"npm run electron\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.3",
    "electron": "^28.2.0"
  }
}
